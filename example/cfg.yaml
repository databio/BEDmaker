metadata:
  sample_table: samples_to_convert.csv
  output_dir: . 
  pipeline_interfaces: ../pipeline_interface.yaml 

derived_attributes: [file_name, file_path]
data_sources:
  source1: test_files/{original_file_name}
  target_file_name: "{original_file_name}.bed"

constant_attributes:
  protocol: "make_bed"
