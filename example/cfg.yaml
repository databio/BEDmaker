metadata:
  sample_table: samples_to_convert.csv
  output_dir: . 
  pipeline_interfaces: ../pipeline_interface.yaml 
derived_attributes: [file_path]

data_sources:
  source1: "files/{file_name}"

constant_attributes:
  protocol: "make_bed"
